#include <iostream>
#include <cstring>
using namespace std;

int main()
{
    char input [100];
    cin >> input;
    int len = strlen(input);
    for(int i=0; i<len; i++){
        cout<< char(*(input + i)+1);
        }
    return 0;
}
